export type cardType = {
  item: {
    type: string
    id: string
    img: string
    system: string
    cpu: string
    gpu: string
    disk: string
    memory: string
    originalPrice: number
    discountPrice: number
    affirmPrice: number
  }
}

export type carouselType = {}
